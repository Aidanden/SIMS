# 🏢 دعم الشركات في نظام محاضر الإتلاف

## ✅ التحديثات المُنفذة

تم تحديث نظام محاضر الإتلاف ليعمل بشكل كامل حسب الشركة - كل شركة ترى فقط أصنافها ومحاضرها.

---

## 🎯 الميزات المُضافة

### 1️⃣ **عزل البيانات حسب الشركة**
- ✅ كل شركة ترى فقط محاضر الإتلاف الخاصة بها
- ✅ كل شركة ترى فقط أصنافها في البحث
- ✅ الـ System User يرى جميع المحاضر والأصناف

### 2️⃣ **تصفية الأصناف الذكية**
- ✅ عرض أصناف الشركة الحالية
- ✅ عرض أصناف الشركة الأم (التقازي - ID=1) إذا كانت متاحة
- ✅ تمييز أصناف الشركة الأم بلون برتقالي
- ✅ عرض badge "مخزن التقازي" للأصناف من الشركة الأم

### 3️⃣ **التحقق من المخزون حسب الشركة**
- ✅ التحقق من الكمية المتوفرة في مخزن الشركة
- ✅ منع إضافة كميات أكبر من المتوفر
- ✅ رسالة خطأ واضحة تعرض الكمية المتاحة والمطلوبة

### 4️⃣ **رقم محضر فريد لكل شركة**
- ✅ صيغة الرقم: `DMG-[كود الشركة]-[السنة][الشهر]-[رقم تسلسلي]`
- ✅ مثال: `DMG-COMP01-202411-0001`
- ✅ الترقيم مستقل لكل شركة

### 5️⃣ **واجهة مستخدم محسّنة**
- ✅ عرض "شركتك" في العنوان للمستخدمين العاديين
- ✅ تمييز الأصناف من الشركة الأم بألوان مختلفة
- ✅ عرض معلومات الشركة في تفاصيل المحضر

---

## 🔧 التغييرات التقنية

### Frontend (`/client/src/app/damage-reports/page.tsx`)

#### 1. إضافة معلومات المستخدم والشركة:
```typescript
// Get current user info
const currentUser = useSelector((state: RootState) => state.auth.user);
const { data: currentUserData } = useGetCurrentUserQuery();
const user = currentUserData?.data || currentUser;
const userCompanyId = user?.companyId;
const isSystemUser = user?.isSystemUser || false;
```

#### 2. تصفية الأصناف حسب الشركة:
```typescript
const { data: productsData } = useGetProductsQuery({
  companyId: userCompanyId,
});
```

#### 3. تصفية نتائج البحث:
```typescript
const filteredProducts = products.filter((product: any) => {
  if (!productSearchTerm) return false;
  
  // Filter by company
  if (!isSystemUser) {
    const isFromUserCompany = product.companyId === userCompanyId;
    const isFromParentCompany = product.createdByCompanyId === 1;
    if (!isFromUserCompany && !isFromParentCompany) {
      return false;
    }
  }
  
  // Search by name or SKU
  const matchesName = product.name.toLowerCase().includes(productSearchTerm.toLowerCase());
  const matchesSku = product.sku.toLowerCase().includes(productSearchTerm.toLowerCase());
  return matchesName || matchesSku;
});
```

#### 4. تمييز أصناف الشركة الأم:
```typescript
const isFromParentCompany = product.createdByCompanyId === 1 && 
                           product.createdByCompanyId !== userCompanyId;

// في الـ UI:
{isFromParentCompany && (
  <span className="bg-orange-100 text-orange-800">
    مخزن التقازي
  </span>
)}
```

### Backend (`/server/src/services/DamageReportService.ts`)

#### التحقق من المخزون حسب الشركة:
```typescript
const stock = await prisma.stock.findFirst({
  where: {
    productId: line.productId,
    companyId: companyId,  // ✅ التحقق من مخزن الشركة المحددة
  },
});

if (!stock || Number(stock.boxes) < line.quantity) {
  return {
    success: false,
    message: `الكمية المتاحة للصنف "${product.name}" غير كافية. 
              المتاح: ${stock?.boxes || 0}، المطلوب: ${line.quantity}`,
  };
}
```

#### خصم الكمية من مخزن الشركة:
```typescript
await prisma.stock.updateMany({
  where: {
    productId: line.productId,
    companyId: companyId,  // ✅ خصم من مخزن الشركة المحددة
  },
  data: {
    boxes: {
      decrement: line.quantity,
    },
  },
});
```

---

## 📊 سيناريوهات الاستخدام

### سيناريو 1: مستخدم عادي (شركة فرعية)
```
المستخدم: أحمد (شركة الفرع الشرقي)
الأصناف المرئية:
  ✅ أصناف شركة الفرع الشرقي
  ✅ أصناف الشركة الأم (التقازي) - مميزة بلون برتقالي
  ❌ أصناف الفروع الأخرى

المحاضر المرئية:
  ✅ محاضر شركة الفرع الشرقي فقط
  ❌ محاضر الشركات الأخرى

رقم المحضر:
  DMG-EAST-202411-0001
```

### سيناريو 2: System User (مدير النظام)
```
المستخدم: مدير النظام
الأصناف المرئية:
  ✅ جميع الأصناف من جميع الشركات

المحاضر المرئية:
  ✅ جميع المحاضر من جميع الشركات

رقم المحضر:
  DMG-[كود الشركة المختارة]-202411-0001
```

### سيناريو 3: الشركة الأم (التقازي)
```
المستخدم: موظف التقازي
الأصناف المرئية:
  ✅ أصناف التقازي فقط

المحاضر المرئية:
  ✅ محاضر التقازي فقط

رقم المحضر:
  DMG-TAQAZI-202411-0001
```

---

## 🔒 الأمان والخصوصية

### 1. عزل البيانات
- ✅ كل شركة معزولة تماماً عن الأخرى
- ✅ لا يمكن لشركة رؤية محاضر شركة أخرى
- ✅ لا يمكن لشركة الوصول لمخزون شركة أخرى

### 2. التحقق من الصلاحيات
- ✅ Backend يتحقق من `companyId` من الـ JWT token
- ✅ لا يمكن التلاعب بالـ `companyId` من Frontend
- ✅ System User فقط يمكنه رؤية جميع البيانات

### 3. التحقق من المخزون
- ✅ التحقق من المخزون يتم على مستوى الشركة
- ✅ لا يمكن إتلاف أصناف من مخزون شركة أخرى
- ✅ رسالة خطأ واضحة عند عدم توفر الكمية

---

## 🎨 الواجهة المرئية

### عرض الأصناف في القائمة المنسدلة:

```
┌─────────────────────────────────────────┐
│ بلاط سيراميك أبيض                      │
│ كود: CER-001 • متوفر: 150.00           │
│                        25.50 د.ل       │
├─────────────────────────────────────────┤
│ بلاط رخام [مخزن التقازي] 🟠           │
│ كود: MAR-001 • متوفر: 80.00            │
│                        45.00 د.ل       │
└─────────────────────────────────────────┘

🟢 أخضر = من شركتك
🟠 برتقالي = من الشركة الأم
```

### رسالة خطأ عند عدم توفر الكمية:

```
❌ الكمية المتاحة للصنف "بلاط سيراميك أبيض" 
   غير كافية.
   
   المتاح: 50.00
   المطلوب: 100.00
```

---

## ✅ الفوائد

- 🔒 **أمان:** عزل كامل بين الشركات
- 🎯 **دقة:** كل شركة ترى بياناتها فقط
- 📊 **وضوح:** تمييز واضح للأصناف من الشركة الأم
- ⚡ **سرعة:** تصفية تلقائية للبيانات
- 💡 **سهولة:** واجهة بديهية وواضحة

---

## 🚀 جاهز للاستخدام!

النظام الآن يدعم الشركات بشكل كامل ويعمل بنفس منطق المبيعات والمشتريات! 🎉

كل شركة معزولة تماماً ولها محاضرها وأصنافها الخاصة.
