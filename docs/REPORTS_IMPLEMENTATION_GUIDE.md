# دليل تحسين شاشة التقارير

## ✅ التحسينات المطبقة

### 1. تحديث API التقارير
تم إضافة endpoints جديدة في `/client/src/state/reportsApi.ts`:
- `getSupplierReport` - تقرير الموردين
- `getPurchaseReport` - تقرير المشتريات

### 2. التقارير الحالية (تعمل بشكل صحيح)
- ✅ تقرير المبيعات
- ✅ تقرير المخزون
- ✅ تقرير الأرباح
- ✅ تقرير العملاء
- ✅ تقرير المنتجات الأكثر مبيعاً

## 🔧 التحسينات المطلوبة على الواجهة

### 1. إصلاح زر الطباعة
المشكلة الحالية: يستخدم `react-to-print` مع `contentRef` (API قديم)

**الحل:**
```typescript
import { useReactToPrint } from "react-to-print";

const handlePrint = useReactToPrint({
  content: () => printRef.current,
  documentTitle: `تقرير_${activeReport}_${new Date().toLocaleDateString("ar-LY")}`,
});
```

### 2. إضافة تقارير الموردين
يجب إضافة التبويبات التالية:
- تقرير الموردين (ملخص الحسابات)
- تقرير المشتريات

### 3. تحسين تنسيق الطباعة
إضافة CSS للطباعة:
```css
@media print {
  body { font-family: 'Cairo', 'Tahoma', sans-serif; }
  .no-print { display: none; }
  table { page-break-inside: avoid; }
  thead { display: table-header-group; }
  tfoot { display: table-footer-group; }
}
```

## 📊 التقارير الجديدة المطلوبة

### تقرير الموردين
**البيانات المطلوبة:**
- اسم المورد
- رقم الهاتف
- إجمالي المشتريات
- إجمالي المدفوع
- الرصيد الحالي
- عدد الفواتير

**Endpoint:** `GET /api/reports/suppliers`

**Query Parameters:**
- `supplierId` (optional)
- `startDate` (optional)
- `endDate` (optional)

### تقرير المشتريات
**البيانات المطلوبة:**
- رقم الفاتورة
- التاريخ
- المورد
- النوع (نقدي/آجل)
- المبلغ
- المصروفات
- الإجمالي النهائي
- الحالة

**Endpoint:** `GET /api/reports/purchases`

**Query Parameters:**
- `supplierId` (optional)
- `startDate` (optional)
- `endDate` (optional)
- `purchaseType` (optional: CASH | CREDIT)

## 🎨 تحسينات الواجهة

### 1. إضافة أيقونات جديدة
```typescript
import { Package, Users, FileText, TrendingUp } from "lucide-react";
```

### 2. تحسين الألوان
- تقرير الموردين: `indigo`
- تقرير المشتريات: `teal`

### 3. إضافة فلاتر متقدمة
- فلتر حسب المورد
- فلتر حسب نوع المشترى
- فلتر حسب الحالة (معتمد/مسودة)

## 🖨️ تحسينات الطباعة

### 1. ترويسة التقرير
```html
<div class="print-header">
  <h1>اسم الشركة</h1>
  <h2>تقرير {اسم التقرير}</h2>
  <p>التاريخ: {التاريخ الحالي}</p>
  <p>الفترة: {من تاريخ} - {إلى تاريخ}</p>
</div>
```

### 2. تذييل التقرير
```html
<div class="print-footer">
  <p>تم الطباعة بواسطة: {اسم المستخدم}</p>
  <p>الصفحة {رقم الصفحة}</p>
</div>
```

### 3. تنسيق الجداول
- إضافة حدود واضحة
- تلوين الصفوف بالتناوب
- إبراز الإجماليات

## 🔨 خطوات التنفيذ

### المرحلة 1: Backend (Server)
1. إنشاء `/server/src/controllers/reports.controller.ts`
2. إضافة routes في `/server/src/routes/reports.routes.ts`
3. إنشاء services للتقارير الجديدة

### المرحلة 2: Frontend (Client)
1. ✅ تحديث `/client/src/state/reportsApi.ts`
2. تحديث `/client/src/app/reports/page.tsx`
3. إضافة مكونات التقارير الجديدة
4. إصلاح وظيفة الطباعة

### المرحلة 3: الاختبار
1. اختبار جميع التقارير
2. اختبار الطباعة
3. اختبار الفلاتر
4. اختبار الأداء

## 📝 ملاحظات مهمة

1. **الطباعة**: استخدام `window.print()` مباشرة أفضل من المكتبات
2. **الأداء**: استخدام pagination للتقارير الكبيرة
3. **التصدير**: إضافة خيار تصدير Excel/PDF
4. **الرسوم البيانية**: استخدام Chart.js للتقارير المرئية

## 🚀 الخطوات التالية

1. إنشاء backend endpoints للتقارير الجديدة
2. تحديث واجهة التقارير
3. إضافة وظيفة طباعة محسّنة
4. اختبار شامل
5. توثيق الاستخدام

## 📞 الدعم

في حالة وجود أي مشاكل:
1. التحقق من console للأخطاء
2. التحقق من network tab للـ API calls
3. التحقق من صلاحيات المستخدم
4. مراجعة logs الخادم
