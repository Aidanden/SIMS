# 🧪 دليل اختبار ميزة التعديل الكامل للفواتير

## 🎯 الهدف من الاختبار
التأكد من عمل جميع وظائف التعديل الكامل للفواتير بشكل صحيح، بما في ذلك:
- تعديل الأصناف الموجودة
- إضافة أصناف جديدة
- حذف أصناف
- التحقق من المخزون
- تحديث الفاتورة بشكل صحيح

---

## 📋 سيناريوهات الاختبار

### 1️⃣ **إعداد البيئة**

#### الخطوة 1.1: تشغيل الخوادم
```bash
# Terminal 1: Backend
cd server
npm run dev

# Terminal 2: Frontend
cd client
npm run dev
```

#### الخطوة 1.2: التحقق من قاعدة البيانات
```bash
# تأكد من وجود بيانات اختبار
cd server
npx prisma studio
```

تحقق من:
- ✅ وجود منتجات في جدول `Product`
- ✅ وجود مخزون كافٍ في جدول `Stock`
- ✅ وجود عملاء في جدول `Customer`
- ✅ وجود فواتير بحالة `DRAFT` في جدول `Sale`

---

### 2️⃣ **اختبار تعديل صنف موجود**

#### السيناريو:
تغيير كمية أو سعر صنف موجود في الفاتورة

#### الخطوات:
1. **افتح شاشة المبيعات أو المحاسب**
   ```
   http://localhost:3000/sales
   أو
   http://localhost:3000/accountant
   ```

2. **ابحث عن فاتورة معلقة (DRAFT)**
   - انظر للفواتير التي لها badge أصفر "معلق"
   - لاحظ الكميات والأسعار الحالية

3. **انقر على زر التعديل (✏️)**
   - يجب أن يظهر modal كبير
   - يجب أن تظهر جميع الأصناف الحالية
   - تحقق من أن الكميات والأسعار مطابقة

4. **عدّل كمية أحد الأصناف**
   ```
   مثال:
   قبل: كمية = 50
   بعد: كمية = 75
   ```

5. **لاحظ تحديث المجموع الجزئي**
   - يجب أن يتحدث المجموع تلقائياً
   - تحقق من الحساب: `الكمية × السعر`

6. **لاحظ المجموع الجديد**
   - يجب أن يتحدث المجموع الكلي
   - قارنه بالمجموع القديم المعروض في الأعلى

7. **احفظ التعديلات**
   - انقر على "حفظ التعديلات"
   - يجب أن تظهر رسالة نجاح
   - يجب أن يُغلق modal تلقائياً

8. **تحقق من النتائج**
   - افتح الفاتورة مرة أخرى للتعديل
   - تحقق من أن الكمية الجديدة محفوظة
   - تحقق من المجموع الجديد

9. **تحقق من المخزون**
   - افتح `prisma studio`
   - ابحث عن الصنف في جدول `Stock`
   - **الفرق المتوقع:**
     ```
     إذا زادت الكمية من 50 إلى 75:
     المخزون القديم: 100 علبة
     بعد الإرجاع: 100 + 50 = 150
     بعد الخصم الجديد: 150 - 75 = 75 علبة
     ```

#### النتيجة المتوقعة:
- ✅ الكمية محدثة في الفاتورة
- ✅ المجموع محدث
- ✅ المخزون محدث بشكل صحيح
- ✅ رسالة نجاح معروضة

---

### 3️⃣ **اختبار حذف صنف من الفاتورة**

#### السيناريو:
حذف صنف كامل من الفاتورة

#### الخطوات:
1. **افتح modal التعديل**
2. **لاحظ عدد الأصناف الحالي**
   ```
   مثال: الأصناف (3)
   ```

3. **انقر على زر الحذف (🗑️) لأحد الأصناف**
   - يجب أن يختفي الصنف فوراً
   - يجب أن يتحدث العداد: `الأصناف (2)`
   - يجب أن يتحدث المجموع الكلي

4. **احفظ التعديلات**
   - انقر على "حفظ التعديلات"
   - يجب أن تظهر رسالة نجاح

5. **تحقق من النتائج**
   - افتح الفاتورة مرة أخرى
   - تحقق من أن الصنف المحذوف غير موجود
   - عدد الأصناف يجب أن يكون (2)

6. **تحقق من المخزون**
   - افتح `prisma studio`
   - ابحث عن الصنف المحذوف في جدول `Stock`
   - **الفرق المتوقع:**
     ```
     إذا حذفت صنف كان بكمية 20:
     المخزون القديم: 50 علبة
     بعد الإرجاع: 50 + 20 = 70 علبة
     (لم يتم خصم شيء لأن الصنف محذوف)
     ```

7. **تحقق من قاعدة البيانات**
   - جدول `SaleLine`: السطر المحذوف يجب أن يكون غير موجود
   - جدول `Sale`: المجموع محدث

#### النتيجة المتوقعة:
- ✅ الصنف محذوف من الفاتورة
- ✅ المجموع محدث بشكل صحيح
- ✅ المخزون مُرجَع بشكل صحيح
- ✅ عدد الأصناف محدث

---

### 4️⃣ **اختبار إضافة صنف جديد**

#### السيناريو:
إضافة صنف جديد لفاتورة موجودة

#### الخطوات:
1. **افتح modal التعديل**
2. **انقر على "إضافة صنف"**
   - يجب أن يظهر صف جديد فارغ
   - يجب أن يتحدث العداد: `الأصناف (4)`

3. **املأ بيانات الصنف الجديد**
   ```
   الصنف: اختر من القائمة المنسدلة
   الكمية: 30
   السعر: 12.50
   ```

4. **لاحظ تحديث المجموع**
   - المجموع الجزئي للسطر: `30 × 12.50 = 375.00`
   - المجموع الكلي: `القديم + 375.00`

5. **احفظ التعديلات**
   - انقر على "حفظ التعديلات"
   - يجب أن تظهر رسالة نجاح

6. **تحقق من النتائج**
   - افتح الفاتورة مرة أخرى
   - تحقق من وجود الصنف الجديد
   - تحقق من الكمية والسعر

7. **تحقق من المخزون**
   - افتح `prisma studio`
   - ابحث عن الصنف الجديد في جدول `Stock`
   - **الفرق المتوقع:**
     ```
     إذا أضفت صنف بكمية 30:
     المخزون القديم: 200 علبة
     بعد الخصم: 200 - 30 = 170 علبة
     ```

#### النتيجة المتوقعة:
- ✅ الصنف الجديد مضاف للفاتورة
- ✅ المجموع محدث بشكل صحيح
- ✅ المخزون مخصوم بشكل صحيح
- ✅ جميع البيانات محفوظة

---

### 5️⃣ **اختبار حذف جميع الأصناف (حالة خطأ)**

#### السيناريو:
محاولة حفظ فاتورة بدون أصناف

#### الخطوات:
1. **افتح modal التعديل**
2. **احذف جميع الأصناف**
   - انقر على زر الحذف لكل صنف
   - يجب أن تصبح القائمة فارغة
   - يجب أن يظهر: `الأصناف (0)`

3. **حاول الحفظ**
   - انقر على "حفظ التعديلات"
   - يجب أن يكون الزر معطّل (disabled)

4. **لاحظ الرسالة**
   - إذا تم تمكين الزر يدوياً (عبر console)
   - يجب أن تظهر رسالة خطأ:
     ```
     ❌ يجب إضافة صنف واحد على الأقل
     ```

#### النتيجة المتوقعة:
- ✅ لا يمكن حفظ فاتورة بدون أصناف
- ✅ زر الحفظ معطّل
- ✅ رسالة خطأ واضحة

---

### 6️⃣ **اختبار المخزون غير الكافي**

#### السيناريو:
محاولة تعديل كمية لتصبح أكبر من المتوفر في المخزون

#### الخطوات:
1. **تحقق من المخزون الحالي**
   ```bash
   # في prisma studio
   # ابحث عن صنف بمخزون قليل
   # مثال: صنف X له 10 علب متاحة
   ```

2. **افتح modal التعديل**
3. **غيّر كمية صنف لقيمة كبيرة**
   ```
   مثال:
   المخزون المتوفر: 10 علب
   الكمية الحالية في الفاتورة: 5 علب
   
   عند الإرجاع سيصبح: 10 + 5 = 15 علبة
   
   حاول تعديل الكمية إلى: 20 علبة
   (أكبر من 15 المتاحة)
   ```

4. **احفظ التعديلات**
   - انقر على "حفظ التعديلات"
   - يجب أن تظهر رسالة خطأ من الـ Backend

5. **لاحظ رسالة الخطأ**
   ```
   ❌ المخزون غير كافي للصنف: [اسم الصنف]
   ```

#### النتيجة المتوقعة:
- ✅ لا يتم الحفظ
- ✅ رسالة خطأ واضحة تذكر اسم الصنف
- ✅ modal يبقى مفتوح للتصحيح
- ✅ لا تتغير أي بيانات

---

### 7️⃣ **اختبار بيانات خاطئة**

#### السيناريو:
إدخال بيانات غير صحيحة (كميات سالبة، أسعار صفرية، إلخ)

#### الخطوات:
1. **افتح modal التعديل**
2. **جرّب إدخال قيم غير صحيحة:**

   **أ. كمية صفرية أو سالبة:**
   ```
   الكمية: 0 أو -5
   ```
   - يجب أن يمنع المتصفح ذلك (min="0.01")
   - إذا تم تجاوزه، يجب أن تظهر رسالة خطأ عند الحفظ

   **ب. سعر صفري أو سالب:**
   ```
   السعر: 0 أو -10
   ```
   - يجب أن يمنع المتصفح ذلك (min="0.01")
   - إذا تم تجاوزه، يجب أن تظهر رسالة خطأ عند الحفظ

   **ج. عدم اختيار صنف:**
   ```
   الصنف: [اختر صنف...]
   ```
   - يجب أن يمنع HTML5 validation الحفظ
   - يجب أن تظهر رسالة من المتصفح

3. **حاول الحفظ**
   - يجب أن تظهر رسالة خطأ:
     ```
     ❌ يرجى التأكد من صحة بيانات جميع الأصناف
     ```

#### النتيجة المتوقعة:
- ✅ لا يتم الحفظ مع بيانات خاطئة
- ✅ رسائل خطأ واضحة
- ✅ المتصفح يساعد في التحقق من الصحة

---

### 8️⃣ **اختبار التعديل المتعدد**

#### السيناريو:
إجراء عدة تعديلات في نفس الوقت

#### الخطوات:
1. **افتح modal التعديل**
2. **قم بالتعديلات التالية معاً:**
   - ✏️ عدّل كمية الصنف الأول: من 10 إلى 15
   - 🗑️ احذف الصنف الثاني
   - ➕ أضف صنف جديد بكمية 25
   - ✏️ عدّل سعر الصنف الثالث: من 10.00 إلى 12.50

3. **لاحظ المجموع الكلي**
   - يجب أن يحتسب جميع التعديلات
   - تحقق يدوياً من الحساب

4. **احفظ التعديلات**
   - انقر على "حفظ التعديلات"
   - يجب أن تظهر رسالة نجاح

5. **تحقق من جميع النتائج**
   - افتح الفاتورة مرة أخرى
   - تحقق من كل تعديل تم إجراؤه
   - تحقق من المجموع الكلي

6. **تحقق من المخزون**
   - افتح `prisma studio`
   - تحقق من المخزون لكل صنف تم تعديله
   - تأكد من صحة الحسابات

#### النتيجة المتوقعة:
- ✅ جميع التعديلات محفوظة
- ✅ المجموع محدث بشكل صحيح
- ✅ المخزون محدث لجميع الأصناف
- ✅ لا توجد تضاربات

---

### 9️⃣ **اختبار تعديل رقم الفاتورة والعميل**

#### السيناريو:
تعديل معلومات الفاتورة الأساسية مع الأصناف

#### الخطوات:
1. **افتح modal التعديل**
2. **لاحظ المعلومات الحالية:**
   ```
   رقم الفاتورة الحالي: INV-001
   العميل الحالي: أحمد محمد
   ```

3. **عدّل رقم الفاتورة:**
   ```
   رقم الفاتورة الجديد: INV-2024-001
   ```

4. **عدّل العميل:**
   ```
   اختر عميل آخر من القائمة
   ```

5. **عدّل بعض الأصناف** (اختياري)
6. **احفظ التعديلات**
7. **تحقق من النتائج:**
   - رقم الفاتورة محدث
   - العميل محدث
   - الأصناف محدثة (إن وُجدت تعديلات)

#### النتيجة المتوقعة:
- ✅ رقم الفاتورة محدث
- ✅ العميل محدث
- ✅ جميع التعديلات محفوظة معاً

---

### 🔟 **اختبار الإلغاء (Cancel)**

#### السيناريو:
إلغاء التعديلات بدون حفظ

#### الخطوات:
1. **افتح modal التعديل**
2. **قم ببعض التعديلات:**
   - عدّل كمية
   - احذف صنف
   - أضف صنف جديد

3. **انقر على "إلغاء"**
   - يجب أن يُغلق modal فوراً
   - يجب ألا يتم حفظ أي تعديلات

4. **افتح modal التعديل مرة أخرى**
   - تحقق من أن البيانات لم تتغير
   - جميع الأصناف كما كانت
   - الكميات والأسعار كما كانت

#### النتيجة المتوقعة:
- ✅ modal يُغلق بدون حفظ
- ✅ لا تتغير أي بيانات
- ✅ يمكن إعادة فتح modal والتعديل مرة أخرى

---

## 🎭 اختبارات الـ UI/UX

### 1. **اختبار Responsive Design**
- افتح modal على شاشات مختلفة:
  - 💻 Desktop (1920x1080)
  - 💻 Laptop (1366x768)
  - 📱 Tablet (768x1024)
  - 📱 Mobile (375x667)
- يجب أن يكون modal قابل للاستخدام على جميع الأحجام

### 2. **اختبار Scrolling**
- أضف 10+ أصناف
- يجب أن تظهر scroll bar داخل قسم الأصناف فقط
- Header يبقى ثابت (sticky)

### 3. **اختبار الأرقام العربية**
- يجب أن تظهر جميع الأرقام بالعربية
- يجب أن يعمل formatting العملة بشكل صحيح

### 4. **اختبار Loading States**
- عند الضغط على "حفظ التعديلات"
- يجب أن يظهر spinner
- يجب أن يُعطّل الزر
- يجب أن يظهر "جار التعديل..."

---

## 📊 جدول النتائج

| # | اسم الاختبار | الحالة | ملاحظات |
|---|--------------|--------|----------|
| 1 | تعديل صنف موجود | ⬜ | |
| 2 | حذف صنف | ⬜ | |
| 3 | إضافة صنف جديد | ⬜ | |
| 4 | حذف جميع الأصناف | ⬜ | |
| 5 | مخزون غير كافٍ | ⬜ | |
| 6 | بيانات خاطئة | ⬜ | |
| 7 | تعديلات متعددة | ⬜ | |
| 8 | تعديل رقم الفاتورة | ⬜ | |
| 9 | إلغاء التعديلات | ⬜ | |
| 10 | UI Responsive | ⬜ | |

**الرموز:**
- ⬜ لم يتم الاختبار
- ✅ نجح
- ❌ فشل
- ⚠️ ملاحظات

---

## 🐛 الأخطاء الشائعة المتوقعة

### 1. **"المخزون غير كافي"**
**السبب:** الكمية المطلوبة أكبر من المتوفر في المخزون بعد إرجاع الكمية القديمة.

**الحل:**
- تحقق من المخزون الفعلي في `prisma studio`
- قلل الكمية المطلوبة
- أو أضف مخزون أولاً

### 2. **"يجب إضافة صنف واحد على الأقل"**
**السبب:** حذف جميع الأصناف من الفاتورة.

**الحل:**
- أضف صنف واحد على الأقل قبل الحفظ

### 3. **"يرجى التأكد من صحة بيانات جميع الأصناف"**
**السبب:** وجود بيانات غير صحيحة (كمية ≤ 0، سعر ≤ 0، صنف غير محدد).

**الحل:**
- تحقق من جميع الحقول المطلوبة
- تأكد من أن جميع القيم > 0

---

## 📝 Checklist النهائي

قبل الانتهاء من الاختبار، تأكد من:

### Backend:
- [ ] API `/api/sales/:id` (PUT) يعمل بشكل صحيح
- [ ] يتم إرجاع المخزون القديم
- [ ] يتم خصم المخزون الجديد
- [ ] يتم حذف الأسطر القديمة
- [ ] يتم إنشاء الأسطر الجديدة
- [ ] يتم تحديث المجموع
- [ ] التحقق من التوفر يعمل
- [ ] رسائل الخطأ واضحة

### Frontend:
- [ ] modal التعديل يفتح بشكل صحيح
- [ ] يتم تحميل الأسطر الحالية
- [ ] إضافة صنف يعمل
- [ ] حذف صنف يعمل
- [ ] تعديل قيم يعمل
- [ ] المجموع يتحدث تلقائياً
- [ ] زر الحفظ يعمل
- [ ] رسائل النجاح/الخطأ تظهر
- [ ] الـ Validation يعمل
- [ ] Loading states تظهر
- [ ] modal يُغلق بعد النجاح
- [ ] البيانات تُحدّث في الجدول

### Database:
- [ ] جدول `Sale` محدث
- [ ] جدول `SaleLine` محدث
- [ ] جدول `Stock` محدث
- [ ] لا توجد تضاربات
- [ ] البيانات متسقة

---

## ✅ معايير النجاح

تُعتبر الميزة ناجحة إذا:

1. ✅ جميع سيناريوهات الاختبار نجحت
2. ✅ لا توجد أخطاء في console
3. ✅ المخزون يُحسب بشكل صحيح
4. ✅ الفواتير تُحدّث بدقة
5. ✅ رسائل الخطأ واضحة ومفيدة
6. ✅ UI بديهية وسهلة الاستخدام
7. ✅ Performance جيد (< 2 ثانية للحفظ)
8. ✅ لا توجد تضاربات في البيانات

---

## 🚀 خطوات ما بعد الاختبار

إذا نجحت جميع الاختبارات:

1. ✅ **توثيق النتائج**
   - املأ جدول النتائج
   - اكتب ملاحظات حول أي مشاكل تم حلها

2. ✅ **Commit التغييرات**
   ```bash
   git add .
   git commit -m "✨ feat: Full invoice edit feature with inventory management"
   ```

3. ✅ **Push إلى Repository**
   ```bash
   git push origin main
   ```

4. ✅ **إنشاء Release Notes**
   - اكتب ملاحظات الإصدار
   - اذكر الميزات الجديدة
   - اذكر التحسينات

5. ✅ **Deploy إلى Production**
   - بعد التأكد من نجاح جميع الاختبارات
   - قم بـ deploy التطبيق

---

## 📞 الدعم

إذا واجهت أي مشاكل أثناء الاختبار:

1. تحقق من console للأخطاء
2. تحقق من Network tab في Developer Tools
3. تحقق من Backend logs
4. راجع التوثيق في `FULL_INVOICE_EDIT_FEATURE.md`

---

**تاريخ الإنشاء:** 2025-11-04  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للاختبار ✅  
**المُعِد:** AI Assistant  
**مرتبط بـ:** `FULL_INVOICE_EDIT_FEATURE.md`

