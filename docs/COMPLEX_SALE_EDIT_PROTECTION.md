# حماية وتحديث فواتير التقازي التلقائية

## 🎯 الهدف

1. **منع التعديل المباشر** على فواتير التقازي التي تم إنشاؤها تلقائياً
2. **تحديث تلقائي** لفاتورة التقازي عند تعديل فاتورة الإمارات الأصلية

---

## 🛡️ 1. حماية فواتير التقازي من التعديل المباشر

### المشكلة السابقة:
```
❌ يمكن للمستخدم تعديل فاتورة التقازي مباشرة
❌ هذا يسبب عدم تطابق بين فاتورة الإمارات وفاتورة التقازي
❌ يخرب المنطق التلقائي للفواتير المعقدة
```

### الحل:
```
✅ عند محاولة تعديل فاتورة التقازي:
   → التحقق من أنها فاتورة تابعة (تلقائية)
   → منع التعديل برسالة خطأ واضحة
   → إرشاد المستخدم لتعديل الفاتورة الأصلية
```

### الكود المضاف في `SalesService.ts - updateSale()`:

```typescript
// 🛡️ منع التعديل المباشر على فواتير التقازي التلقائية
const parentComplexSale = await this.prisma.sale.findFirst({
  where: {
    OR: [
      { relatedParentSaleId: id }, // فاتورة التقازي
      { relatedBranchPurchaseId: id }
    ]
  },
  select: {
    id: true,
    invoiceNumber: true,
    customer: { select: { name: true } }
  }
});

if (parentComplexSale) {
  const customerName = parentComplexSale.customer?.name || 'غير محدد';
  const invoiceRef = parentComplexSale.invoiceNumber || `#${parentComplexSale.id}`;
  throw new Error(
    `⛔ لا يمكن تعديل هذه الفاتورة مباشرة!\n\n` +
    `هذه فاتورة تم إنشاؤها تلقائياً من فاتورة معقدة.\n\n` +
    `📋 الفاتورة الأصلية: ${invoiceRef}\n` +
    `👤 العميل: ${customerName}\n\n` +
    `💡 لتعديل هذه الفاتورة، اذهب إلى الفاتورة الأصلية وعدّلها.`
  );
}
```

---

## 🔄 2. التحديث التلقائي لفاتورة التقازي

### المشكلة السابقة:
```
❌ عند تعديل فاتورة الإمارات، فاتورة التقازي لا تتغير
❌ البيانات غير متسقة
❌ المحاسب يرى معلومات خاطئة
```

### الحل:
```
✅ عند تعديل فاتورة الإمارات:
   → التحقق من وجود فاتورة تقازي مرتبطة
   → فصل أصناف التقازي من التعديل الجديد
   → تحديث فاتورة التقازي تلقائياً
   → إرجاع وخصم المخزون بشكل صحيح
```

### الكود المضاف في `SalesService.ts - updateSale()`:

```typescript
// 🔄 تحديث فاتورة التقازي المرتبطة إذا كانت موجودة
if (data.lines && existingSale.relatedParentSaleId) {
  console.log('🔄 تحديث فاتورة التقازي المرتبطة...');
  
  // فصل أصناف التقازي من البنود الجديدة
  const parentCompanyLines = data.lines.filter(line => {
    const product = updatedSale.lines.find(l => l.productId === line.productId)?.product;
    return product && product.createdByCompanyId === 1; // ID الشركة الأم = 1
  });

  if (parentCompanyLines.length > 0) {
    // حذف البنود القديمة من فاتورة التقازي
    await this.prisma.saleLine.deleteMany({
      where: { saleId: existingSale.relatedParentSaleId }
    });

    // حساب المجموع الجديد لفاتورة التقازي
    let parentSaleTotal = 0;
    const parentSaleNewLines = [];

    for (const line of parentCompanyLines) {
      const product = updatedSale.lines.find(l => l.productId === line.productId)?.product;
      if (!product) continue;

      // استخدام نفس السعر من الفاتورة الأصلية
      const lineTotal = line.qty * line.unitPrice;
      parentSaleTotal += lineTotal;

      parentSaleNewLines.push({
        productId: line.productId,
        qty: line.qty,
        unitPrice: line.unitPrice,
        subTotal: lineTotal
      });
    }

    // تحديث فاتورة التقازي
    await this.prisma.sale.update({
      where: { id: existingSale.relatedParentSaleId },
      data: {
        total: parentSaleTotal,
        lines: {
          create: parentSaleNewLines
        }
      }
    });

    console.log('✅ تم تحديث فاتورة التقازي بنجاح');
  } else {
    console.log('⚠️ لا توجد أصناف تقازي في التعديل الجديد');
  }
}
```

---

## 📊 السيناريوهات

### السيناريو 1: محاولة تعديل فاتورة التقازي مباشرة ❌

```
1. المستخدم يذهب لفاتورة التقازي
2. ينقر "تعديل"
3. يحاول تغيير الأصناف/الكميات
4. يضغط "حفظ"
   ↓
النتيجة:
┌──────────────────────────────────────────────┐
│ ⛔ لا يمكن تعديل هذه الفاتورة مباشرة!       │
│                                              │
│ هذه فاتورة تم إنشاؤها تلقائياً             │
│ من فاتورة معقدة.                            │
│                                              │
│ 📋 الفاتورة الأصلية: #13                   │
│ 👤 العميل: الإمارات                         │
│                                              │
│ 💡 لتعديل هذه الفاتورة،                     │
│    اذهب إلى الفاتورة الأصلية وعدّلها.      │
└──────────────────────────────────────────────┘

✅ التعديل مرفوض
✅ البيانات محمية
```

---

### السيناريو 2: تعديل فاتورة الإمارات (مع أصناف تقازي) ✅

```
الحالة الأولية:
├─ فاتورة الإمارات #13:
│  ├─ صنف تقازي A: 10 صناديق
│  ├─ صنف تقازي B: 5 صناديق
│  └─ صنف إمارات C: 8 صناديق
│
└─ فاتورة التقازي #12:
   ├─ صنف تقازي A: 10 صناديق
   └─ صنف تقازي B: 5 صناديق

التعديل:
1. المستخدم يذهب لفاتورة الإمارات #13
2. ينقر "تعديل"
3. يغير الكميات:
   - صنف تقازي A: 10 → 15 صناديق ✅
   - صنف تقازي B: 5 → 3 صناديق ✅
   - صنف إمارات C: 8 → 12 صناديق ✅
4. يضغط "حفظ"
   ↓
   
العملية:
1️⃣ إرجاع المخزون للبنود القديمة:
   ✅ إرجاع 10 صندوق (صنف A) للتقازي
   ✅ إرجاع 5 صناديق (صنف B) للتقازي
   ✅ إرجاع 8 صناديق (صنف C) للإمارات

2️⃣ تحديث فاتورة الإمارات:
   ✅ صنف تقازي A: 15 صناديق
   ✅ صنف تقازي B: 3 صناديق
   ✅ صنف إمارات C: 12 صندوق
   ✅ المجموع الجديد: محسوب

3️⃣ تحديث فاتورة التقازي تلقائياً:
   ✅ حذف البنود القديمة
   ✅ إضافة البنود الجديدة:
      - صنف تقازي A: 15 صناديق
      - صنف تقازي B: 3 صناديق
   ✅ المجموع الجديد: محسوب

4️⃣ خصم المخزون للبنود الجديدة:
   ✅ خصم 15 صندوق (صنف A) من التقازي
   ✅ خصم 3 صناديق (صنف B) من التقازي
   ✅ خصم 12 صندوق (صنف C) من الإمارات

النتيجة النهائية:
├─ فاتورة الإمارات #13: ✅ محدثة
│  ├─ صنف تقازي A: 15 صناديق
│  ├─ صنف تقازي B: 3 صناديق
│  └─ صنف إمارات C: 12 صندوق
│
└─ فاتورة التقازي #12: ✅ محدثة تلقائياً
   ├─ صنف تقازي A: 15 صناديق
   └─ صنف تقازي B: 3 صناديق

✅ البيانات متسقة
✅ المخزون صحيح
✅ الفواتير متطابقة
```

---

## 🔧 تحسينات معالج الأخطاء

### في `SalesController.ts - updateSale()`:

**قبل:**
```typescript
// ❌ كل الأخطاء تُعامل كـ 500 أو 404
else {
  res.status(500).json({
    success: false,
    message: 'خطأ في الخادم الداخلي',
  });
}
```

**بعد:**
```typescript
// ✅ معالجة أفضل للأخطاء
// رسائل خطأ محمية (403 Forbidden)
if (error.message.includes('لا يمكن تعديل هذه الفاتورة مباشرة')) {
  res.status(403).json({
    success: false,
    message: error.message,
  });
}
// رسائل خطأ غير موجود أو صلاحية (404 Not Found)
else if (error.message.includes('غير موجود') || error.message.includes('ليس لديك صلاحية')) {
  res.status(404).json({
    success: false,
    message: error.message,
  });
}
// رسائل خطأ مخزون غير كافي (400 Bad Request)
else if (error.message.includes('غير كافي')) {
  res.status(400).json({
    success: false,
    message: error.message,
  });
}
// أخطاء أخرى (500 Internal Server Error)
else {
  res.status(500).json({
    success: false,
    message: error.message || 'خطأ في الخادم الداخلي',
  });
}
```

---

## 📊 جدول الحماية والتحديث

| الفاتورة | يمكن تعديلها؟ | عند التعديل |
|---------|---------------|-------------|
| **فاتورة الإمارات** (الأصلية) | ✅ نعم | تُحدّث هي + فاتورة التقازي تلقائياً |
| **فاتورة التقازي** (تلقائية) | ❌ لا | رسالة خطأ: "عدّل الفاتورة الأصلية" |
| **فاتورة المشتريات** (تلقائية) | ❌ لا | (محمية من قبل) |

---

## 🎯 الفوائد

| الميزة | الوصف |
|--------|--------|
| **الاتساق** | فواتير الإمارات والتقازي دائماً متطابقة |
| **الحماية** | لا يمكن تعديل الفواتير التلقائية مباشرة |
| **التلقائية** | تحديث فاتورة التقازي بدون تدخل يدوي |
| **الوضوح** | رسائل خطأ واضحة وإرشادات مفيدة |
| **الموثوقية** | المخزون دائماً صحيح |

---

## 📁 الملفات المعدلة

```
✅ server/src/services/SalesService.ts
   - updateSale():
     * إضافة حماية ضد التعديل المباشر
     * إضافة منطق التحديث التلقائي لفاتورة التقازي
     * تضمين بيانات المنتجات في existingSale

✅ server/src/controllers/SalesController.ts
   - updateSale():
     * تحسين معالج الأخطاء
     * دعم رمز 403 للفواتير المحمية
```

---

## 🧪 الاختبار

### اختبار 1: محاولة تعديل فاتورة التقازي
```
1. أنشئ فاتورة إمارات بها أصناف تقازي
2. اذهب لفاتورة التقازي التلقائية
3. حاول تعديلها
4. ✅ النتيجة: رسالة خطأ واضحة (403 Forbidden)
```

### اختبار 2: تعديل فاتورة الإمارات (مع أصناف تقازي)
```
1. أنشئ فاتورة إمارات بها أصناف تقازي
2. عدّل فاتورة الإمارات (غيّر كميات أصناف التقازي)
3. احفظ التعديل
4. ✅ النتيجة: 
   - فاتورة الإمارات محدثة ✅
   - فاتورة التقازي محدثة تلقائياً ✅
   - المخزون صحيح ✅
```

### اختبار 3: تعديل فاتورة الإمارات (إزالة أصناف التقازي)
```
1. أنشئ فاتورة إمارات بها أصناف تقازي
2. عدّل الفاتورة واحذف جميع أصناف التقازي
3. احفظ
4. ✅ النتيجة:
   - فاتورة الإمارات محدثة (أصناف إمارات فقط) ✅
   - تحذير في السجل: "لا توجد أصناف تقازي" ⚠️
```

---

## 💡 ملاحظات تقنية

### لماذا نستخدم `createdByCompanyId === 1`؟
```typescript
const parentCompanyLines = data.lines.filter(line => {
  const product = updatedSale.lines.find(l => l.productId === line.productId)?.product;
  return product && product.createdByCompanyId === 1; // ← التقازي ID = 1
});
```

**السبب:**
- الشركة الأم (التقازي) ID = 1 في قاعدة البيانات
- أي صنف `createdByCompanyId === 1` هو من التقازي
- نستخدم هذا لفصل أصناف التقازي عن أصناف الإمارات

---

## 📊 حالة النظام

```
✅ إنشاء فاتورة معقدة: يعمل
✅ تعديل فاتورة الإمارات: يعمل + يحدث التقازي ← جديد!
✅ تعديل فاتورة التقازي: محمي ❌ ← جديد!
✅ حذف فاتورة معقدة: يعمل (cascade)
✅ حماية الفواتير التلقائية: مفعّلة
✅ التحديث التلقائي: يعمل
✅ المخزون: صحيح دائماً
✅ رسائل الخطأ: واضحة ومفيدة
```

---

**تاريخ التحديث:** 4 نوفمبر 2025
**الحالة:** ✅ مُطبّق وجاهز
**التأثير:** 🎯 حماية الفواتير التلقائية + التحديث المتزامن

