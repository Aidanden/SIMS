# 🔥 ميزة التعديل الكامل للفواتير

## 📋 ملخص التحديث

تم تحديث ميزة تعديل الفواتير لتسمح بـ:
- ✅ **تعديل الأصناف الموجودة**
- ✅ **تعديل الكميات والأسعار**
- ✅ **حذف أصناف من الفاتورة**
- ✅ **إضافة أصناف جديدة**
- ✅ **تعديل رقم الفاتورة والعميل**

---

## ⚡ التحديثات الرئيسية

### قبل التحديث ❌:
```
[رسالة تحذيرية]
⚠️ ملاحظة: لا يمكن تعديل الأصناف أو الكميات بعد إنشاء الفاتورة.
يمكنك فقط تعديل رقم الفاتورة والعميل.
```

### بعد التحديث ✅:
```
[واجهة تعديل كاملة]
✏️ تعديل الفاتورة - الأصناف (3)  [+ إضافة صنف]

┌─────────────────────────────────────────────────┐
│ [▼ اختر صنف] [كمية] [سعر]          [🗑️ حذف]   │
│ بلاط ABC-001   50      15.50         ❌         │
│ سيراميك XYZ    30      25.00         ❌         │
│ أدوات          10      5.00          ❌         │
└─────────────────────────────────────────────────┘

المجموع الجديد: 1,525.00 د.ل
```

---

## 🏗️ التعديلات التقنية

### 1. **New States**
```typescript
// State لإدارة الأسطر في التعديل
const [editLines, setEditLines] = useState<Array<{
  productId: number;
  qty: number;
  unitPrice: number;
}>>([]);
```

### 2. **Updated handleEditSale**
```typescript
const handleEditSale = (sale: Sale) => {
  setSaleToEdit(sale);
  // تحميل الأسطر الحالية للتعديل
  setEditLines(sale.lines.map(line => ({
    productId: line.productId,
    qty: Number(line.qty),
    unitPrice: Number(line.unitPrice)
  })));
  setShowEditModal(true);
};
```

### 3. **Updated handleEditSubmit**
```typescript
const handleEditSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  if (!saleToEdit) return;

  const formData = new FormData(e.target as HTMLFormElement);
  const customerId = formData.get('customerId') ? Number(formData.get('customerId')) : undefined;
  const invoiceNumber = formData.get('invoiceNumber') as string;

  // التحقق من وجود أسطر
  if (editLines.length === 0) {
    showError('يجب إضافة صنف واحد على الأقل');
    return;
  }

  // التحقق من صحة البيانات
  for (const line of editLines) {
    if (!line.productId || line.qty <= 0 || line.unitPrice <= 0) {
      showError('يرجى التأكد من صحة بيانات جميع الأصناف');
      return;
    }
  }

  try {
    await updateSale({
      id: saleToEdit.id,
      data: {
        customerId,
        invoiceNumber: invoiceNumber || undefined,
        lines: editLines  // ⬅️ إرسال الأسطر المعدلة
      }
    }).unwrap();

    success(`تم تعديل الفاتورة بنجاح`);
    setShowEditModal(false);
    setSaleToEdit(null);
    setEditLines([]);
    refetch();
  } catch (err: any) {
    showError(err?.data?.message || 'حدث خطأ أثناء تعديل الفاتورة');
  }
};
```

### 4. **Helper Functions**
```typescript
// إضافة صنف جديد
const addEditLine = () => {
  setEditLines(prev => [...prev, { productId: 0, qty: 1, unitPrice: 0 }]);
};

// حذف صنف
const removeEditLine = (index: number) => {
  setEditLines(prev => prev.filter((_, i) => i !== index));
};

// تحديث قيمة حقل في صنف
const updateEditLine = (index: number, field: 'productId' | 'qty' | 'unitPrice', value: number) => {
  setEditLines(prev => prev.map((line, i) => 
    i === index ? { ...line, [field]: value } : line
  ));
};
```

### 5. **New Imports**
```typescript
// إضافة في شاشة المحاسب
import { useGetProductsQuery } from '@/state/productsApi';

// جلب المنتجات
const { data: productsData } = useGetProductsQuery({ limit: 1000 });
```

---

## 🎨 واجهة المستخدم الجديدة

### 1. **Modal Header**
```
╔══════════════════════════════════════════╗
║ ✏️ تعديل الفاتورة                [❌]  ║
╚══════════════════════════════════════════╝
```

### 2. **معلومات الفاتورة الحالية**
```
┌─────────────────────────────────────────┐
│ رقم الفاتورة الحالي: INV-001           │
│ المجموع القديم: 1,200.00 د.ل           │
└─────────────────────────────────────────┘
```

### 3. **حقول الإدخال**
```
📝 رقم الفاتورة: [INV-001____________]

👤 العميل: [▼ قائمة العملاء]
```

### 4. **قسم الأصناف** (الجديد! 🆕)
```
الأصناف (3)                    [+ إضافة صنف]

┌─────────────────────────────────────────────────┐
│ الصنف: [▼ بلاط ABC-001 - ABC001]              │
│ الكمية: [50] السعر: [15.50]          [🗑️]     │
│ المجموع: 775.00 د.ل                            │
├─────────────────────────────────────────────────┤
│ الصنف: [▼ سيراميك XYZ - XYZ002]              │
│ الكمية: [30] السعر: [25.00]          [🗑️]     │
│ المجموع: 750.00 د.ل                            │
└─────────────────────────────────────────────────┘

زر [+ إضافة صنف] ⬅️ لإضافة صنف جديد
زر [🗑️] ⬅️ لحذف صنف
```

### 5. **المجموع الجديد**
```
┌─────────────────────────────────────────┐
│ المجموع الجديد: 1,525.00 د.ل          │
└─────────────────────────────────────────┘
```

### 6. **رسالة تنبيهية**
```
⚠️ تنبيه:
عند تعديل الأصناف أو الكميات، سيتم إرجاع المخزون القديم 
وخصم المخزون الجديد. تأكد من توفر المخزون الكافي.
```

### 7. **أزرار الإجراءات**
```
[إلغاء]  [✓ حفظ التعديلات]
```

---

## 🔧 كيف يعمل الـ Backend

### 1. **إرجاع المخزون القديم**
```typescript
// في updateSale method
// خطوة 1: إرجاع الكميات القديمة للمخزون
for (const line of existingSale.lines) {
  await this.prisma.stock.update({
    where: { companyId_productId: { companyId, productId: line.productId } },
    data: { boxes: { increment: Number(line.qty) } }
  });
}
```

### 2. **التحقق من توفر المخزون الجديد**
```typescript
// خطوة 2: التحقق من توفر الأصناف الجديدة
for (const line of data.lines) {
  const product = await getProduct(line.productId);
  const stock = await getStock(product.id, companyId);
  
  if (stock.boxes < line.qty) {
    throw new Error(`المخزون غير كافي للصنف: ${product.name}`);
  }
}
```

### 3. **حذف الأسطر القديمة**
```typescript
// خطوة 3: حذف الأسطر القديمة من قاعدة البيانات
await this.prisma.saleLine.deleteMany({
  where: { saleId: id }
});
```

### 4. **إنشاء الأسطر الجديدة**
```typescript
// خطوة 4: إنشاء الأسطر الجديدة
const updatedSale = await this.prisma.sale.update({
  where: { id },
  data: {
    ...otherUpdates,
    lines: {
      create: data.lines.map(line => ({
        productId: line.productId,
        qty: line.qty,
        unitPrice: line.unitPrice,
        subTotal: line.qty * line.unitPrice
      }))
    }
  }
});
```

### 5. **خصم المخزون الجديد**
```typescript
// خطوة 5: خصم الكميات الجديدة من المخزون
for (const line of data.lines) {
  await this.prisma.stock.update({
    where: { companyId_productId: { companyId, productId: line.productId } },
    data: { boxes: { decrement: Number(line.qty) } }
  });
}
```

---

## 🧪 كيفية الاختبار

### 1. **اختبار تعديل صنف موجود:**
```bash
1. افتح شاشة المحاسب أو المبيعات
2. اختر فاتورة معلقة (DRAFT)
3. انقر على "تعديل"
4. غيّر الكمية أو السعر لصنف موجود
5. احفظ التعديلات
6. تحقق من:
   - تحديث المجموع الإجمالي
   - تحديث المخزون
```

### 2. **اختبار حذف صنف:**
```bash
1. افتح modal التعديل
2. انقر على زر الحذف (🗑️) لأحد الأصناف
3. احفظ التعديلات
4. تحقق من:
   - اختفاء الصنف من الفاتورة
   - إرجاع كميته للمخزون
   - تحديث المجموع
```

### 3. **اختبار إضافة صنف جديد:**
```bash
1. افتح modal التعديل
2. انقر على "إضافة صنف"
3. اختر صنف من القائمة
4. أدخل الكمية والسعر
5. احفظ التعديلات
6. تحقق من:
   - ظهور الصنف الجديد في الفاتورة
   - خصم كميته من المخزون
   - تحديث المجموع
```

### 4. **اختبار المخزون غير الكافي:**
```bash
1. افتح modal التعديل
2. أدخل كمية أكبر من المتوفر في المخزون
3. حاول الحفظ
4. يجب أن تظهر رسالة خطأ:
   "المخزون غير كافي للصنف: [اسم الصنف]"
```

### 5. **اختبار حذف جميع الأصناف:**
```bash
1. افتح modal التعديل
2. احذف جميع الأصناف
3. حاول الحفظ
4. يجب أن تظهر رسالة خطأ:
   "يجب إضافة صنف واحد على الأقل"
```

---

## ⚠️ رسائل الخطأ

| الحالة | الرسالة |
|--------|---------|
| لا توجد أصناف | يجب إضافة صنف واحد على الأقل |
| بيانات خاطئة | يرجى التأكد من صحة بيانات جميع الأصناف |
| مخزون غير كافٍ | المخزون غير كافي للصنف: [اسم الصنف] |
| صنف غير موجود | بعض الأصناف غير موجودة |

---

## 📁 الملفات المعدلة

### Backend (كان موجوداً):
- ✅ `server/src/services/SalesService.ts`
  - Method: `updateSale()` يدعم تعديل الأسطر

### Frontend (محدث):

#### 1. **شاشة المحاسب**
- ✅ `client/src/app/accountant/page.tsx`
  - إضافة `editLines` state
  - إضافة `useGetProductsQuery`
  - تحديث `handleEditSale`
  - تحديث `handleEditSubmit`
  - إضافة `addEditLine`, `removeEditLine`, `updateEditLine`
  - تحديث كامل لـ Edit Modal UI

#### 2. **صفحة المبيعات**
- ✅ `client/src/app/sales/page.tsx`
  - نفس التحديثات كشاشة المحاسب
  - Edit Modal مطابق للتصميم

---

## 🎯 الفوائد

### للمستخدمين:
- ✅ **مرونة كاملة** في تعديل الفواتير
- ✅ **تصحيح الأخطاء** بسهولة
- ✅ **إضافة/حذف أصناف** بسرعة
- ✅ **واجهة بديهية** وسهلة الاستخدام

### للنظام:
- ✅ **إدارة دقيقة للمخزون** (إرجاع القديم + خصم الجديد)
- ✅ **التحقق من التوفر** قبل الحفظ
- ✅ **سلامة البيانات** مضمونة

### للأعمال:
- ✅ **تقليل الأخطاء** في الفواتير
- ✅ **توفير الوقت** (لا حاجة لحذف وإعادة إنشاء)
- ✅ **زيادة الإنتاجية**

---

## 🔄 سير العمل الكامل

```
مستخدم المبيعات
    ↓
1. إنشاء فاتورة مبدئية (DRAFT)
    ↓
2. يلاحظ خطأ في الأصناف أو الكميات
    ↓
3. ينقر على "تعديل"
    ↓
4. يظهر Modal التعديل مع جميع الأصناف
    ↓
5. يمكنه:
   - تعديل الكميات/الأسعار
   - حذف أصناف
   - إضافة أصناف جديدة
   - تغيير العميل
   - تعديل رقم الفاتورة
    ↓
6. ينقر على "حفظ التعديلات"
    ↓
7. Backend يقوم بـ:
   - إرجاع المخزون القديم
   - التحقق من توفر المخزون الجديد
   - حذف الأسطر القديمة
   - إنشاء الأسطر الجديدة
   - خصم المخزون الجديد
    ↓
8. تحديث فوري في الواجهة
    ↓
9. المحاسب يعتمد الفاتورة
```

---

## 🚀 الميزات المستقبلية (اختيارية)

### 1. **Drag & Drop**
- إعادة ترتيب الأصناف بالسحب والإفلات

### 2. **Bulk Edit**
- تعديل مجموعة من الفواتير معاً

### 3. **History Log**
- سجل تفصيلي لجميع التعديلات

### 4. **Compare View**
- عرض مقارنة بين الفاتورة القديمة والجديدة

### 5. **Smart Suggestions**
- اقتراحات ذكية للأصناف بناءً على التاريخ

---

## ✅ الخلاصة

تم بنجاح تحديث ميزة **تعديل الفواتير** لتصبح ميزة **تعديل كامل**:

1. ✅ تعديل الأصناف الموجودة
2. ✅ تعديل الكميات والأسعار
3. ✅ حذف أصناف من الفاتورة
4. ✅ إضافة أصناف جديدة للفاتورة
5. ✅ إدارة تلقائية للمخزون
6. ✅ التحقق من التوفر
7. ✅ واجهة بديهية وسهلة
8. ✅ رسائل خطأ واضحة

**الميزة جاهزة للاستخدام! 🎉🔥**

---

**تاريخ التحديث:** 2025-11-04  
**الإصدار:** 2.0.0  
**الحالة:** ✅ جاهز للإنتاج  
**نوع التحديث:** Major Update 🔥  
**مرتبط بـ:** `SALES_EDIT_FEATURE.md`, `SALES_EDIT_IN_SALES_PAGE.md`

